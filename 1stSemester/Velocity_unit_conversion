#include <stdio.h>
int main()
{
    float VK, VM;
    printf("Enter velocity in km/hr");
    scanf("%f", &VK);
    VM = VK * 5 / 18.0; 
    printf("Equivalent velocity is %6.2f m/sec\n", VM);
    return 0;
}
